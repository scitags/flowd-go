pidPath: "./flowd-go.pid"
workDir: "."
stunServers:
  - "stun:stun.l.google.com:19305"
  - "stun:stun.services.mozilla.org:3478"

plugins:
  np:
    pipePath: foo

backends:
  firefly:
    destinationPort: 10515
